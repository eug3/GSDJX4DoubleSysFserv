<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 基本网络权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 传统蓝牙权限 (API 30 及以下) -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />

    <!-- Android 12+ 蓝牙权限 (API 31+) -->
    <!-- 声明 neverForLocation 表示应用不通过蓝牙进行地理定位，有助于提高隐私合规性 -->
    <uses-permission 
        android:name="android.permission.BLUETOOTH_SCAN" 
        android:usesPermissionFlags="neverForLocation" 
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- 位置权限 (某些 BLE 扫描场景仍需，特别是在旧版本 Android 上) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- 前台服务权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- 前台服务权限 (API 31+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" tools:targetApi="s" />
    
    <!-- 通知权限 (API 33+，Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- 悬浮窗权限 -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>

    <application
        android:name=".X4Application"
        android:allowBackup="true"
        android:largeHeap="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GSDJX4DoubleSysFserv"
        android:extractNativeLibs="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GSDJX4DoubleSysFserv">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DeviceScanActivity"
            android:label="扫描设备"
            android:theme="@style/Theme.GSDJX4DoubleSysFserv" />
        <activity
            android:name=".GeckoActivity"
            android:exported="false"
            android:label="阅读器"
            android:theme="@style/Theme.GSDJX4DoubleSysFserv" />
        
        <!-- 命令服务：处理蓝牙命令和 HTTP 请求 -->
        <service
            android:name=".CommandService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />

        <!-- BLE 命令接收器 -->
        <receiver
            android:name=".BleCommandReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.guaishoudejia.x4doublesysfserv.BLE_COMMAND" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
